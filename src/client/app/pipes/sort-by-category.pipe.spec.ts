import { SortByCategoryPipe } from './sort-by-category.pipe';

describe('SortByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
